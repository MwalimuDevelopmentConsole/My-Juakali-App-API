services:
  api:
    image: ghcr.io/${GITHUB_REPOSITORY}:production
    container_name: radient-api
    restart: always
    ports:
      - "127.0.0.1:3501:3501"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./storage:/app/storage
      - ./logs:/app/logs
