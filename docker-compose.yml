services:
  api:
    image: ghcr.io/${GITHUB_REPOSITORY}:production
    container_name: craftory-api
    restart: always
    ports:
      - "127.0.0.1:3500:3500" # Map host 3500 to container 3500
    env_file:
      - .env
    volumes:
      - ./uploads:/craftory/uploads
      - ./storage:/craftory/storage
      - ./logs:/craftory/logs